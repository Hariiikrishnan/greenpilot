# Base URL of the ForgeChat MCP API (note the /api/mcp/v1 suffix).
FORGECHAT_API_URL=https://crm.srv879786.hstgr.cloud/api/mcp/v1

# A bearer key minted in ForgeChat → Admin Settings → MCP Tools (shown once).
FORGECHAT_API_KEY=fck_live_PASTE_YOUR_KEY
